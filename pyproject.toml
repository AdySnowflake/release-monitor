[project]
name = "release-monitor"
version = "1.1.1"
description = "监控 GitHub Release，通过 AI 自动筛选并下载对应平台的发布文件"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai",
    "langchain",
    "langchain-openai",
    "requests",
    "python-dotenv",
    "tzdata",
]
